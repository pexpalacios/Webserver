- Added a directory in /www fro the images and cgis that's be used in the web. 
- May need to do two different pages? Not sure.
- ^ Subject says server should listen in different ports for different content, so at least two pages are needed. 
But that could also be the menu page and then the game or something.

Errors to make:
- Empty config file throws exception V
- Only server and no vars should throw exception
- Wrong values not trhowing exception
- Putting "on" "off" in autoindex and protected make no difference in printlocation
- It says location block is not closed if left like "location /x {}" because it doesn't find the closing one
- Check handling of file permissions, both config file and mmaybe directories it should access? 